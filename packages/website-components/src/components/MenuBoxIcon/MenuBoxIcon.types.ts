import type { StackProps } from '@mui/material';

export interface MenuBoxIconProps extends StackProps {
  iconIdle: string;
  iconHover: string;
  headline: string;
  text: string;
  link: string;
}
