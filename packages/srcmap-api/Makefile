build-SrcMapApiFunction:
	npm run build
	cp dist/* $(ARTIFACTS_DIR)

build-SrcMapFetcherFunction:
	npm run build
	cp dist/* $(ARTIFACTS_DIR)

build-SrcMapFetcherDeadLetterQueueFunction:
	npm run build
	cp dist/* $(ARTIFACTS_DIR)
