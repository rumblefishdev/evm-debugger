import { Card, styled } from '@mui/material'

export const StyledBox = styled(Card)(() => ({
    width: '100%',
    padding: '24px',
    maxWidth: '584px',
    height: '100%',
    borderRadius: '18px',
}))
