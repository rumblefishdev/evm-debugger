import React from 'react'

import { Styled{{ pascalCase name }} } from './{{ pascalCase name }}.styles'
import type { {{ pascalCase name }}Props } from './{{ pascalCase name }}.types'

export const {{ pascalCase name }}: React.FC<{{ pascalCase name }}Props> = (props) => {
  return <Styled{{ pascalCase name }} {...props}>{{ pascalCase name }}</Styled{{ pascalCase name }}>
}
